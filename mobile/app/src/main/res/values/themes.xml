<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Activity theme. Compose supplies the real UI theme (Material3);
         this just needs to be a valid, action-bar-less window theme. -->
    <style name="Theme.YtcEntry" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
