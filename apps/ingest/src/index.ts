// Email ingestion worker — entrypoint. Implemented in Task #9.
// Polls ytc.entry@gmail.com over IMAP, dedupes on Message-ID, stores images,
// runs the matcher, writes PhotoSubmission rows. See plan §9.
console.log("ytc ingest: not yet implemented (Task #9)");
