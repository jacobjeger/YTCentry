{
  "name": "@ytc/pusher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloud push worker (Railway). Drains the PushJob queue and writes to the E16C through the Access-gated Cloudflare tunnel (https://door.<domain>). Replaces the on-site agent.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@ytc/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
